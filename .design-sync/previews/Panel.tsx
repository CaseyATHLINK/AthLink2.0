import React from 'react';
import { Panel } from '@athlink/design-system';

export const Default = () => (
  <Panel>
    <div style={{ padding: 16 }}>
      <div style={{ fontWeight: 800, marginBottom: 6 }}>Fleet standings</div>
      <div style={{ color: 'var(--mut)', fontSize: 13 }}>
        Frosted-glass surface used to group tables and sections of content.
      </div>
    </div>
  </Panel>
);
