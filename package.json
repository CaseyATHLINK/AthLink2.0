{
  "name": "athlink",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @athlink/web dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "create-sport": "node tools/create-sport.mjs"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
